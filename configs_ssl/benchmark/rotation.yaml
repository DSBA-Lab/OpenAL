DEFAULT:
  seed: 0
  savedir: ./results/batch/rotation
  exp_name: default
DATASET:
  datadir: /datasets
  batch_size: 1024
  test_batch_size: 512
  num_workers: 12
  aug_info: null
OPTIMIZER:
  name: SGD
  lr: 0.1
  params:
    momentum: 0.9
    weight_decay: 5e-4
TRAIN:
  epochs: 120
  grad_accum_steps: 1
  mixed_precision: fp16
  log_interval: 10
  ckp_metric: acc
  wandb:
    use: true
    entity: dsba-al-2023
    project_name: Active Learning - Benchmark (SSL rotation)
MODEL:
  name: resnet18
  pretrained: false